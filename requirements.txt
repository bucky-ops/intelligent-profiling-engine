# Runtime dependencies for the Intelligent Profiling Engine.
# Pinned to real, currently-available PyPI versions (regenerated from the
# audit - the previous file contained fictional future pins such as
# torch==2.10.0, pyspark==4.1.1, transformers==4.57.6 which do not exist
# on PyPI and caused `pip install -r requirements.txt` to fail).
#
# Install with:  pip install -r requirements.txt
# Dev / test:    pip install -r requirements-dev.txt

scikit-learn>=1.3,<2
pandas>=2.0,<3
numpy>=1.24,<3
matplotlib>=3.7,<4
spacy>=3.7,<4
textblob>=0.17,<1
sentence-transformers>=2.2,<4
PyYAML>=6.0,<7
Faker>=20.0,<41
streamlit>=1.30,<2
